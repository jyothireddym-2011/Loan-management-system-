# The ML package has ZERO third-party dependencies by design (pure Python
# Gaussian Naive Bayes implementation). This file is kept so the module can
# grow independently of the backend's requirements.txt.
#
# Uncomment if/when the model is upgraded to a scikit-learn implementation:
# scikit-learn>=1.4,<2.0
# numpy>=1.26,<2.0
